# Elemento de maior valor

import numpy as np

import numpy as np

imagem = np.random.randint(low=0,
                           high=256,
                           size=(800, 600),
                           dtype=np.uint8)

print(imagem)