# Elemento de maior valor

import numpy as np

import numpy as np

array = np.array([[4, 2, 1],
                  [6, 4, 2]])

print(array)
print(array.shape)

array = np.expand_dims(array, axis=0)

print(array)
print(array.shape)